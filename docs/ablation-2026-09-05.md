# Hermes-dispatch 消融审查与 QQ 文件交付实验

## 固定基线与范围

- 远程 `main`: `c0486902026bcf6cd14e05bdf0b1f85da88e04aa`；PR #1/#2/#4/#5/#6 均已合并。
- 审查近期提交：`git diff 938db08...c048690`。QQ 文件交付缺口属于此前已有代码的能力缺口，不归因为这些提交引入的回归。
- 执行时上游 Hermes 源码：`1bbb6e5bc`（源码版本 0.20.5），Python 3.11。回归使用独立 HERMES_HOME，避免生产插件覆盖受测副本。
- 规范依据 AGENTS.md；需求依据插件 README 与本次用户要求。

## 规范审查

没有确认的规范硬违规。四处可安全移除的冗余经逐项消融验证：

| 实验 | 唯一变化 | 结果 | 处理 |
|---|---|---|---|
| A1 | 删除最终收尾中不可达的重复无 carrier 分支 | 3 个 QQ 脚本全通过 | 保留删除 |
| A2 | 删除只做 re-raise 的 CancelledError 分支 | 3 个 QQ 脚本全通过 | 保留删除 |
| A3 | 删除 unmark 后第二次 prune | 保留状态相同，扫描 2→1；3 脚本全通过 | 保留删除 |
| A4 | 删除 prune 的可选参数递归路径 | remove 扫描 2→1；3 脚本全通过 | 保留删除 |
| 负对照 | 单独副本删除 asyncio.shield | cancelled-holder 唯一交付断言失败 | 不保留 |

每项保留后均运行 `test_streaming.py`、`test_final_delivery.py`、`test_steer.py`；分别有 82/15/27 个成功场景标记（不是断言数量）。新增 public send 回归覆盖期限封口后已提交正文与空 final。运行代码净减 23 行；增加 20 行回归。未删除 broker、tombstone、delta provenance、steer barrier 或历史数据迁移。

## 功能审查与文件消融

确认的缺口：Codex 在最终回答中输出本地 Markdown 链接或 `:codex-file-citation{... purpose="output"}`，而 Hermes 流式收尾只消费 `MEDIA:`。QQ 本身已有原生文件上传实现。

| 实验 | 输入/变化 | 结果 |
|---|---|---|
| F0 | 原 runtime 的本地下载链接，经真实 Gateway/QQ 代码 | 上传 0 字节，回归红 |
| F1 | 同一文件仅改为交付标记 | upload_prepare → PUT → upload_part_finish → files → messages；字节一致 |
| F2 | 只追加标记，保留本地链接 | 非流式重复上传，负对照红；方案舍弃 |
| F3 | 转换交付引用、使用文件名作为标签，复用上游路径验证 | 私聊/群聊、流式/非流式均恰一次上传 |
| F4 | 真实 QQ 第二次基线输出 output citation | 旧实现与仅支持 Markdown 的实现都漏传；补 output citation 后回归绿 |
| F5 | runtime 转换成功上传，但原文已流式发出 | 真实 C 出现重复说明，舍弃 runtime 改写 |
| F6 | 转換后移至 QQAdapter.extract_media | 两种 dispatch 恰一次上传；Codex runtime 不变，最终 D 真实下载通过 |

HTTP 为离线回归的唯一传输替身；Gateway 提取、普通响应处理、QQ send_document、分块读取/校验和上传编排均为真实代码。还覆盖空格/中文、重复引用、代码/引用文本、source citation、远程/坏链接、缺失文件、敏感路径及符号链接、其他平台及历史使用的 Base 提取器不变。没有新增上传服务、依赖或提示词。

未证实且不在本次修改中的审查线索：soft eviction 在 turn/start 尚未返回时可能缺少活动标记；session_project 每轮可能重复持久化相同 thread。没有公共回归证明前不删除这两部分。

## 真实 QQ 验收

用户授权在本机 QQ 自然语言交互；仅使用采购机器人私聊及虚构物料。通过 `/new` 清除旧会话上下文。模型保持原设置（QQ 显示 gpt-5.6-luna-900k/openai-codex）。不在测试消息中提示媒体标记、接口或指定上传工具。

- 08:37 初次中文键入被 QQ 丢字；完整英文更正触发 steer。A.csv 只出现本地链接，作为探索，不作为干净对照。
- 08:40 干净基线：`Please make a CSV purchase list with columns Item, Quantity, Notes. Add screws, 10, test and washers, 20, test. Name the file purchase-list-B.csv and send it to me when ready.`
- 08:41 基线完成：QQ 原样显示 output citation，未出现附件卡片；日志确认流式 final 已结束。文件真实存在。
- 08:49 C：runtime 实验版出现 51 B 文件卡片；QQ 下载成功，CSV 三列两行与请求一致。SHA-256：`057e7f3a148ccdfc356708ecb3ff581c0022b7f4d4dd1ffc172826e2ce8f9d61`，下载与生成文件字节完全相同。但原 citation 与规范化正文重复，因此最终版本撤销 runtime 改写。
- 08:59 最终版本 D：QQ 显示一份 `purchase-list-D.csv` 文件卡片，下载成功。51 B，SHA-256 与 C 上述值相同，三列两行内容完全正确。Codex DB 中最终回答只有 output citation、没有 MEDIA 指令；QQ 显示只保留原流式说明，没有第二份规范化正文。Gateway 日志记录真实分片上传。
- 09:01 普通文本文件 E：模型只输出 Markdown 下载链接；QQ 出现唯一 `packing-checklist-E.txt` 文件卡片，下载成功。43 B，三行分别为 `Check quantity`、`Protect corners`、`Seal carton`。SHA-256：`533033e8ba0840b18dde4c044e57df1e8efde488388ae915237db65abdd65fdd`，下载与源文件字节完全一致。
- Hermes 自身后台 self-improvement 在基线之后报告创建/更新 artifact-delivery skill；本任务未更改测试提示、模型、技能或记忆。真实 A/B 存在模型非确定性，确定性归因由同字节离线消融补足。

## 启用与回滚

QQ 插件 README 已记录原因、范围、安装、验证、回滚。用户在自动审批首次拦截后明确授权“允许执行”。中间 Codex 插件 1.8.4 已完成备份及 canary 实验；最终撤销这条方案，恢复原 Codex 插件 1.8.3，仅由 qqbot-connect-hotfix 1.8.22 持有修复与精简。每次重启前检查 active_agents=0。默认/product、配置、凭据、WhatsApp 插件保持不变。

```bash
scripts/install-plugins.sh "$HOME/.hermes/profiles/procurement" qqbot-connect-hotfix
hermes -p procurement gateway restart
scripts/install-plugins.sh --restore "$HOME/.hermes/profiles/procurement" qqbot-connect-hotfix <backup-directory>
hermes -p procurement gateway restart
```

最终 15 个插件测试脚本及安装器回归全部通过（16/16）。关闭 QQ 输出解析包装的负对照准确失败于 `local output never uploaded to QQ`。15 个受保护文件哈希全部保持不变；运行插件源码与受测副本相符。生产范围仅 procurement，Codex 插件保持原 1.8.3，QQ 插件为 1.8.22。默认/product 未更新。未推送或发布远程仓库。

### 具体回滚记录（本地 canary）

- 原 Codex 1.8.3 备份：`$HOME/.hermes/profiles/procurement/plugin-backups/codex-app-server-phase-hotfix-1.8.3-20260905-004556`，已恢复。
- 原 QQ 1.8.21 备份：`$HOME/.hermes/profiles/procurement/plugin-backups/qqbot-connect-hotfix-1.8.21-20260905-005643`。
- 最终 Gateway 在 08:57:16 QQ Ready；其后 D 自然语言请求完成原生附件交付。

限制：当前流式正文仍可能显示 Codex 原始文件引用标记；真实文件在其后的 QQ 附件卡片中。未把普通路径、源码引用或历史工具输出一概上传。真实群聊没有发测试消息，群聊路径验证仅来自隔离的真实代码/HTTP替身回归。未做吞吐压测，扫描次数下降不等于已测得总体响应时间提升。
